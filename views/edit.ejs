<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900 p-10 text-white">
        <a class="text-blue-500 inline-block mb-10" href="/">Go Back Home</a>
        
        <!-- Combined form for updating filename and task -->
        <form action="/edit" method="POST">
            <!-- Filename section -->
            <input class="block w-1/4 rounded-md outline-none px-5 py-2 bg-zinc-800 text-zinc-500" 
                   type="text" 
                   placeholder="Previous name" 
                   name="previous" 
                   value="<%= filename %>" readonly>

            <input class="block w-1/4 rounded-md outline-none mt-2 px-5 py-2 bg-zinc-800" 
                   type="text" 
                   placeholder="New Filename" 
                   name="updated">

            <!-- Task content section -->
            <textarea class="block w-full h-60 rounded-md outline-none px-5 py-2 bg-zinc-800 mt-10 resize-none align-top text-zinc-500" 
                      placeholder="Previous data" 
                      name="previous_data" readonly><%= filedata %></textarea>

            <textarea class="block w-full rounded-md outline-none mt-2 px-5 py-2 resize-none bg-zinc-800 h-60" 
                      placeholder="New data" 
                      name="updated_data"></textarea>

            <!-- Single submit button for updating both task and name -->
            <input class="mt-2 bg-yellow-600 px-5 py-2 rounded-md" 
                   type="submit" 
                   value="Update Task">
        </form>
    </div>
</body>
</html>
